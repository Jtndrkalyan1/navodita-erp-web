{
  "name": "navodita-erp-web",
  "version": "1.0.0",
  "description": "Navodita ERP - Enterprise Resource Planning System",
  "main": "backend/src/index.js",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "migrate": "cd backend && npm run migrate",
    "postinstall": "cd backend && npm install --production"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["erp", "express", "postgresql", "react"],
  "private": true
}
